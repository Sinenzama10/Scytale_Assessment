# src/config.py

import os

# Fetch the GitHub token from the environment variables
GITHUB_TOKEN = os.getenv('GITHUB_TOKEN')

# Organization name as a constant; adjust as needed
ORG_NAME = "Scytale-exercise"
